# Пошаговая инструкция по настройке CI/CD

## Что такое CI/CD простыми словами?

**CI/CD** = автоматический деплой при каждом push в Git.

**Было:**
1. Делаете изменения
2. Push в Git
3. Заходите на сервер
4. `git pull`
5. `./scripts/deploy.sh`
6. Ждёте 2 минуты

**Стало:**
1. Делаете изменения
2. Push в Git
3. **Всё остальное происходит автоматически!** 🎉

## Шаг 1: Подготовка SSH ключа

На сервере создайте SSH ключ специально для GitHub Actions:

```bash
# На сервере выполните:
ssh-keygen -t rsa -b 4096 -C "github-actions" -f ~/.ssh/github_actions
```

**Важно:** Не используйте пароль (просто нажмите Enter).

Теперь скопируйте **приватный** ключ:

```bash
cat ~/.ssh/github_actions
```

Скопируйте **весь вывод** (включая `-----BEGIN RSA PRIVATE KEY-----` и `-----END RSA PRIVATE KEY-----`).

## Шаг 2: Настройка секретов в GitHub

1. Откройте ваш репозиторий: https://github.com/BaczQ/payload-cms-site

2. Перейдите: **Settings** → **Secrets and variables** → **Actions**

3. Нажмите **New repository secret** и добавьте:

### PAYLOAD_SECRET
```
Значение: ваш секретный ключ (минимум 32 символа)
Можно взять из .env файла на сервере или сгенерировать:
openssl rand -base64 32
```

### DATABASE_URI
```
Значение: строка подключения к PostgreSQL
Формат: postgresql://user:password@host:5432/database
Можно взять из .env файла на сервере
```

### NEXT_PUBLIC_SERVER_URL
```
Значение: https://bfnews.ru
```

### SERVER_HOST
```
Значение: IP-адрес или домен вашего сервера
Пример: 123.45.67.89 или bfnews.ru
```

### SERVER_USER
```
Значение: root
(или ваш пользователь, если не root)
```

### SSH_PRIVATE_KEY
```
Значение: приватный ключ, который вы скопировали на шаге 1
(весь вывод команды cat ~/.ssh/github_actions)
```

## Шаг 3: Настройка авторизованных ключей на сервере

Добавьте публичный ключ в authorized_keys:

```bash
# На сервере:
cat ~/.ssh/github_actions.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

Проверьте, что подключение работает:

```bash
# На вашем компьютере (не на сервере):
# Создайте временный файл с приватным ключом
nano /tmp/test_key
# Вставьте приватный ключ, сохраните (Ctrl+O, Enter, Ctrl+X)

# Проверьте подключение:
ssh -i /tmp/test_key root@YOUR_SERVER_IP

# Если подключение работает, удалите тестовый файл:
rm /tmp/test_key
```

## Шаг 4: Активация workflow

Файл `.github/workflows/deploy.yml` уже создан в проекте.

Просто закоммитьте и запушьте:

```bash
cd /var/www/bf-news
git add .github/workflows/deploy.yml
git commit -m "Add CI/CD workflow"
git push origin main
```

## Шаг 5: Проверка работы

1. Откройте GitHub: https://github.com/BaczQ/payload-cms-site
2. Перейдите в раздел **Actions** (вкладка вверху)
3. Вы увидите запущенный процесс "Build and Deploy"
4. Кликните на него, чтобы увидеть детали

Если всё настроено правильно, вы увидите:
- ✅ Зелёные галочки на каждом шаге
- ✅ Сообщение "Deploy completed successfully"

## Что происходит при каждом push

1. **GitHub получает код** из репозитория
2. **Собирает проект** в чистой среде (Ubuntu + Node.js 20 + pnpm)
3. **Создаёт артефакты** (.next/standalone, .next/static, public)
4. **Загружает на сервер** через SSH
5. **Устанавливает production зависимости**
6. **Выполняет миграции** базы данных
7. **Перезапускает приложение** через PM2

Всё это занимает ~1-2 минуты.

## Отладка проблем

### Ошибка: "Permission denied (publickey)"

**Проблема:** SSH ключ не настроен правильно.

**Решение:**
1. Проверьте, что приватный ключ правильно скопирован в GitHub Secrets
2. Проверьте, что публичный ключ добавлен в `~/.ssh/authorized_keys` на сервере
3. Проверьте права: `chmod 600 ~/.ssh/authorized_keys`

### Ошибка: "Build failed"

**Проблема:** Проект не собирается.

**Решение:**
1. Проверьте логи в GitHub Actions
2. Убедитесь, что все секреты (PAYLOAD_SECRET, DATABASE_URI) правильно настроены
3. Попробуйте собрать локально: `pnpm build`

### Ошибка: "deploy-production.sh: Permission denied"

**Проблема:** Скрипт не имеет прав на выполнение.

**Решение:**
```bash
# На сервере:
chmod +x /var/www/bf-news/scripts/deploy-production.sh
```

### Ошибка: "pnpm: command not found"

**Проблема:** pnpm не установлен на сервере.

**Решение:**
```bash
# На сервере:
npm install -g pnpm@10
```

## Полезные команды

### Просмотр логов деплоя:
```bash
# На сервере:
pm2 logs bf-news
```

### Проверка статуса:
```bash
# На сервере:
pm2 status
```

### Ручной запуск деплоя (если нужно):
```bash
# На сервере:
cd /var/www/bf-news
./scripts/deploy-production.sh
```

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте секреты в Git
- Используйте только GitHub Secrets для хранения паролей и ключей
- `.env` файл должен быть в `.gitignore`
- SSH ключи храните только в GitHub Secrets

## Готово!

Теперь при каждом push в `main` или `master` ветку, ваш проект будет автоматически собираться и деплоиться на сервер.

**Попробуйте:**
1. Сделайте небольшое изменение (например, добавьте комментарий в код)
2. Закоммитьте: `git commit -am "Test CI/CD"`
3. Запушьте: `git push origin main`
4. Откройте GitHub Actions и наблюдайте за магией! ✨

